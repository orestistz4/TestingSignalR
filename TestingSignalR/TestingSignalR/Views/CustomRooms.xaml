<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:HeaderVieww="clr-namespace:TestingSignalR.Views.ContentViews"
             x:Class="TestingSignalR.Views.CustomRooms">
    <Shell.TitleView>
        <ContentView>

            <HeaderVieww:HeaderView/>

        </ContentView>
    </Shell.TitleView>
    <ContentPage.Content>
        
        <StackLayout>
            <Button Text="Join" Command="{Binding JoinCommand}"/>
            <Button Text="Register" Command="{Binding CreateCommand}"/>
            <Button Text="Your Rooms" Command="{Binding YourRoomsCommand}" />
            <Button Text="Create" Command="{Binding CreateRoomCommand}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>